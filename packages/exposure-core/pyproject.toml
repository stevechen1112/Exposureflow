[project]
name = "exposureflow-core"
version = "0.1.0"
description = "Exposure opportunity scoring, topic graph, SERP slot logic"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["exposure_core"]
