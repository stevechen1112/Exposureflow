---
description: ExposureFlow 憲章四原則 — 唯一執行準則，禁止 MVP/縮減範圍
alwaysApply: true
---

# ExposureFlow 憲章（強制）

完整版：`AGENTS.md`。以下四點為使用者訂立的最高原則，必須逐字遵循。

## 原則 1：按計畫 Phase 0→14 逐步實作

完成當前 Phase 驗收後才進下一 Phase。**每次進入新 Phase 前**必須重新閱讀 `docs/product/exposureflow-development-plan.md`（第九章當前 Phase + 第十六章補強），並執行 Phase Kickoff（見 `AGENTS.md` §進入新 Phase 前）。禁止憑記憶寫碼、禁止偏離計畫。僅必要時可調整（記錄於 phase-log），禁止跳 Phase、禁止 MVP 捷徑。

## 原則 2：每 Phase → **完整 Code Review** → 修復 → Commit

驗收單位 = 一個 Phase。完整 Code Review **必須**包含：

1. EF-xxxx 逐項對照（含第十六章補強票）
2. `ruff` + `pytest` **實際執行**（0 failed；有程式變更須跑 Postgres 整合測試）
3. 租戶隔離 / connector contract 測試（若適用）
4. **Bugbot** 子代理 review（High/Critical 必須修復）
5. **Security Review**（auth/憑證/多租戶/計費等 Phase 必做）
6. `docs/product/phase-N-review.md` 八章節證據齊全

禁止：僅填 PASS 表、測試 skip 卻標 PASS、未跑 Bugbot 就結案。

通過後直接進下一 Phase。**不需**每 Phase 向使用者確認。

## 原則 3：完整作法

目標是 Phase 0–14 全部完成的完整產品。**禁止** MVP、精簡版、stub、最小做法、省略測試/migration/tenant/RBAC。

## 原則 4：自主推進，完整實作後才回報

Phase 0 起持續工作，過程中不請示、不回報進度。僅 secret/破壞性操作/無法推斷的商業決策可暫停。Session 斷開時完成當前 Phase commit 後續接。**唯一回報**：Phase 0–14 全部完成後一次總報告。

## 禁止

MVP、MVP-Dev、Beta/GA 分層交付、先簡後全、Phase 內做一部分冒充完成、自評 Code Review 不通過仍標 completed。

## 規格與紀錄

- 計畫：`docs/product/exposureflow-development-plan.md`
- 進度：`docs/product/phase-log.md`
- Review 範本：`AGENTS.md` §完整 Code Review 流程、`phase-N-review.md` 八章節
- 技術棧：FastAPI + PostgreSQL + Celery + Next.js 15 + Clerk + Stripe
